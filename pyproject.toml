[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "manual-annotator"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "streamlit",
    "streamlit-option-menu",
    "pandas",
]
